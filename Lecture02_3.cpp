#include <iostream>
using namespace std;

int main()
{
    // char grade = 'N';
    // int value = grade;

    // cout << value << endl;

    // double price = 100.99;
    // int newPrice = (int)price;

    // typecasting
    //  cout << (5 / (double)2) << endl;

    // relational operator (==, !=, <, <=, >=, >)
    cout << (3 < 5) << endl;    //true -> 1

    return 0;
}